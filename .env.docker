# Environment configuration for Docker deployment
# Copy this to .env and fill in your values

# Node Environment
NODE_ENV=production

# Server Configuration
PORT=8080

# Database
DATABASE_PATH=/app/data/nourish.sqlite

# Session Configuration
# Generate with: openssl rand -base64 32
SESSION_SECRET=your-secret-key-here-at-least-32-chars

# Admin Credentials
ADMIN_USERNAME=admin
# Generate with: bun run hash.js your-password
ADMIN_PASSWORD_HASH=your-bcrypt-hash-here

# CORS Configuration
# In production with Docker, this should be the URL where users access the app
# For local testing: http://localhost:8080
# For production: https://your-domain.com
ALLOWED_ORIGIN=http://localhost:8080

# VAPID Keys for Push Notifications (Optional)
# Generate with: npx web-push generate-vapid-keys
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=
