# Admin credentials
# Option 1: Use password hash (recommended for production)
# Generate password hash with: echo -n "your_password" | node hash.js
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=$2a$10$your_generated_bcrypt_hash_here

# Option 2: Use plain text password (for development only)
# If ADMIN_PASSWORD_HASH is set, it takes precedence. Otherwise, ADMIN_PASSWORD is used.
# ADMIN_PASSWORD=admin

# Session secret (use a random 32+ character string)
# Generate with: openssl rand -base64 32
SESSION_SECRET=your_random_session_secret_min_32_chars_long_here

# Database path (relative or absolute)
DATABASE_PATH=./data/nourish.sqlite

# VAPID keys for push notifications (optional)
# If not configured, push notifications will be disabled.
# Generate with: npx web-push generate-vapid-keys
# All three keys must be set together, or none.
# VAPID_PUBLIC_KEY=BExamplePublicKeyHere123
# VAPID_PRIVATE_KEY=ExamplePrivateKeyHere456
# VAPID_SUBJECT=mailto:your-email@example.com

# CORS allowed origin (frontend URL)
ALLOWED_ORIGIN=http://localhost:5173

# Insecure mode: disable strict origin checks and relax CSP connect-src to allow tunnels/CDNs.
# ONLY enable when running behind a trusted reverse proxy or Cloudflare Tunnel.
# Accepted values: true|false (default false)
INSECURE_DISABLE_ORIGIN_CHECKS=false

# Environment
NODE_ENV=development

# Server port
PORT=8080

# API token for secure API access (optional)
# Generate a strong random token with: openssl rand -hex 32
# API_TOKEN=your_random_api_token_here
